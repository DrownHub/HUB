local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Drown Hub v1", "BloodTheme")

local Main = Window:NewTab("Main")
local MainSection = Main:NewSection("Main")

MainSection:NewButton("Dupe Beli(Need 50k beli or more", "Gonna Dupe Your Beli", function()
    if nil the
    else
    game.Players.LocalPlayer:kick("Your beli not enough")
    end
end)
